---
description: Live studio is not this git repo
alwaysApply: true
---

The live studio tree may lag git. Prefer a PR to `master`.

- Do not start a new Vercel project to "fix" the split.
- Do not invent a second public site.
- GitHub is the bus. Update `HANDOFF.md` and the PR.
