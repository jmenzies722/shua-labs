"use client";
import { motion, useMotionValue, useReducedMotion, useSpring, useTransform } from "framer-motion";
export function Hero(){const reduced=useReducedMotion();const x=useSpring(useMotionValue(0),{stiffness:55,damping:20});const y=useSpring(useMotionValue(0),{stiffness:55,damping:20});const a=useTransform(x,v=>v*45);const b=useTransform(y,v=>v*45);return <section id="top" className="hero liquid-hero" onPointerMove={e=>{if(!reduced){x.set(e.clientX/window.innerWidth-.5);y.set(e.clientY/window.innerHeight-.5)}}}><motion.div className="liquid-orb orb-one" style={{x:a,y:b}} aria-hidden/><div className="liquid-noise" aria-hidden/><motion.div initial={{opacity:0,y:reduced?0:28}} animate={{opacity:1,y:0}} className="shell hero-content"><div className="glass-chip">Shua Labs <span/> New York</div><h1>Building what<br/><em>comes next.</em></h1><p className="hero-copy">Shua Labs creates ventures, products, and systems for an AI-native world.</p><a className="button glass-button" href="#focus">Enter the studio <span>↓</span></a></motion.div></section>}
